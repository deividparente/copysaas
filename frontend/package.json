{
    "name": "raiar-mensagens-frontend",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "next dev",
        "build": "prisma generate && prisma migrate deploy && next build",
        "start": "next start",
        "lint": "next lint",
        "postinstall": "cd ../backend && npm install && npx prisma generate"
    },
    "dependencies": {
        "axios": "^1.7.9",
        "next": "13.5.6",
        "react": "^18",
        "react-dom": "^18"
    },
    "devDependencies": {
        "@types/node": "^20",
        "@types/react": "^18",
        "@types/react-dom": "^18",
        "typescript": "^5"
    }
}