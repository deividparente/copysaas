# Database (SQLite - funciona automaticamente no Netlify)
DATABASE_URL="file:./dev.db"

# JWT Secret (será gerado automaticamente se não fornecido)
JWT_SECRET="auto-generated-secret-change-in-production"

# Frontend URL (detectado automaticamente)
FRONTEND_URL=""

# Node Environment
NODE_ENV="production"
