{
    "buildCommand": "cd backend && npm install && npx prisma generate && npx prisma migrate deploy && cd ../frontend && npm install && npm run build",
    "outputDirectory": "frontend/.next",
    "installCommand": "npm install",
    "env": {
        "DATABASE_URL": {
            "description": "URL de conexão do banco de dados (PostgreSQL)",
            "required": true
        },
        "JWT_SECRET": {
            "description": "Chave secreta JWT - gere com: openssl rand -hex 32",
            "required": true
        },
        "FRONTEND_URL": {
            "description": "URL do frontend (preenchida automaticamente)",
            "required": false
        },
        "NEXT_PUBLIC_API_URL": {
            "description": "URL da API pública",
            "required": false
        },
        "NODE_ENV": {
            "value": "production"
        }
    }
}